<%# Render comment %>
$('#comments_<%= @post.id %>').prepend("<%= j render @comment %>");
<%# Remove comment description in comment form %>
document.getElementById('comment_description_<%= @post.id %>').value = '';
