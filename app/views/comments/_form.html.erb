<%= form_with(model: [post, Comment.new], local: true) do |f| %>
  <%= f.text_field :description, autofocus: true, autocomplete: "off", placeholder: "Écrire un commentaire", class: "form-control mr-1" %>
  <span class="input-group-append"><%= f.submit 'Envoyer', class: 'btn btn-primary' %></span>
<% end %>
