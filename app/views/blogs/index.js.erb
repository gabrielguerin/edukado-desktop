<%# Render blogs %>
$("#blogs").html("<%= escape_javascript(render("articles")) %>");
<%# Render pagination %>
$("#paginate").html("<%= escape_javascript(paginate(@blogs, remote: true)) %>");
<%# Scroll to top of window %>
$(document).ready(function(){
$(this).scrollTop(0);
});
