
<%= form_for(@level, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.form_group :level, class: 'row' do |f| %>
      <%= f.label :level, class: 'control-label col-md-2' %>
      <div class='col-12 col-md-10'>
        <%= f.number_field :level, class: 'form-control' %>
        <%= f.error_messages %>
      </div>
    <% end %>

  </div>

  <div class="d-flex">
    <div class="ml-auto">
      <%= f.submit class: 'btn btn-primary' %>
      <%= link_to "Cancel", levels_path, class: 'btn btn-outline-default' %>
    </div>
  </div>
<% end %>

